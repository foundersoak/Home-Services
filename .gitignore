# dependencies / build output
node_modules
dist
.vite

# os / editor
.DS_Store
*.local
*.log

# dev scratch: build-verification screenshots + one-off scripts.
# Reusable tooling lives in tools/ (re-included below).
*.png
*.mjs
_*.html
!tools/**
